// This file is auto-generated by @hey-api/openapi-ts

import type { RequestResult } from '../client';
import { client } from '../client.gen';
import type { Options } from '../sdk.gen';
import type { BanStatusData, BanStatusErrors, BanStatusResponses, BanUsersData, BanUsersErrors, BanUsersResponses, ListBansData, ListBansErrors, ListBansResponses, UnbanUsersData, UnbanUsersErrors, UnbanUsersResponses } from '../types.gen';

export class MessageAdminBanService {
    /**
     * 封禁用户（按服务）
     *
     * 批量封禁用户（审核联动）。
     *
     * 按服务维度（comment / dm）封禁，给出理由与封禁时长
     * （temporary + duration_days 限时 / permanent 永久）。
     * 按服务逐项鉴权：请求含 comment 需 comment 域 BAN 位、含 dm 需 dm 域 BAN 位。
     */
    public static banUsers<ThrowOnError extends boolean = false>(options: Options<BanUsersData, ThrowOnError>): RequestResult<BanUsersResponses, BanUsersErrors, ThrowOnError, 'data'> {
        return (options.client ?? client).post<BanUsersResponses, BanUsersErrors, ThrowOnError, 'data'>({
            responseStyle: 'data',
            url: '/api/v1/message/admin/ban',
            ...options,
            headers: {
                'Content-Type': 'application/json',
                ...options.headers
            }
        });
    }
    
    /**
     * 解封用户（按服务）
     *
     * 批量解封用户（审核联动）。
     *
     * 不传 `ban_services` 时解封该用户全部服务（需拥有 comment:ban 与 dm:ban 全部权限）；
     * 传则仅解除指定服务，并逐项校验对应权限。
     */
    public static unbanUsers<ThrowOnError extends boolean = false>(options: Options<UnbanUsersData, ThrowOnError>): RequestResult<UnbanUsersResponses, UnbanUsersErrors, ThrowOnError, 'data'> {
        return (options.client ?? client).post<UnbanUsersResponses, UnbanUsersErrors, ThrowOnError, 'data'>({
            responseStyle: 'data',
            url: '/api/v1/message/admin/unban',
            ...options,
            headers: {
                'Content-Type': 'application/json',
                ...options.headers
            }
        });
    }
    
    /**
     * 封禁记录列表
     *
     * 分页查看封禁记录（root 或持有用户域查看位的管理员）。
     */
    public static listBans<ThrowOnError extends boolean = false>(options?: Options<ListBansData, ThrowOnError>): RequestResult<ListBansResponses, ListBansErrors, ThrowOnError, 'data'> {
        return (options?.client ?? client).get<ListBansResponses, ListBansErrors, ThrowOnError, 'data'>({
            responseStyle: 'data',
            url: '/api/v1/message/admin/ban/list',
            ...options
        });
    }
    
    /**
     * 查询用户封禁状态
     *
     * 查询某用户在各服务的封禁状态（实时计算到期）。
     */
    public static banStatus<ThrowOnError extends boolean = false>(options: Options<BanStatusData, ThrowOnError>): RequestResult<BanStatusResponses, BanStatusErrors, ThrowOnError, 'data'> {
        return (options.client ?? client).get<BanStatusResponses, BanStatusErrors, ThrowOnError, 'data'>({
            responseStyle: 'data',
            url: '/api/v1/message/admin/ban/status',
            ...options
        });
    }
}
